<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="in.canews.zeronetmobile.common_python">

    <application
        android:hasCode="false"/>

    <dist:module
        dist:instant="false"
        dist:title="@string/title_common">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>
</manifest>

